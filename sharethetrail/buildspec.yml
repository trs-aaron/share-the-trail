version: 0.1

phases:
  pre_build:
    commands:
      - pip install -r requirements.txt
      - python manage.py collectstatic
  build:
    commands:
  post_build:
    commands:
      - echo Build completed on `date`